
{{ form_start(form) }}
<div class="row">
    <div class="col-8">
    {{ form_widget(form) }}
    </div>
</div>
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
