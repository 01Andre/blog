{% block body %}
<h1>
    On a ajouté un article sur votre blog:
</h1>
    <p>Ce message vous a été envoyé par {{ expeditorEmail }}</p>
    <br>
    {{ article.title }} a été passé dans la catégorie {{ article.category }}.
<br>
    Le contenu de l'article est le suivant : {{ article.content }}<br>
    <a href="{{ url('blog_show', { 'articleSlug': article.slug} )  }}">Pour voir le nouvel article cliquez ici!</a>
{% endblock %}