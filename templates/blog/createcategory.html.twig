{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="text-center m-4">Ajouter une catégorie</h1>
    {{ form_start(form) }}
    <div class="row justify-content-center">
        <div class="col-2 m-4">
            {{ form_row(form.name) }}
        </div>
    </div>
    <div class=" row justify-content-center">

            <button class=" col-1 m-2 btn btn-dark">{{ button_label|default('Ajouter') }}</button>

    </div>

    {{ form_end(form) }}



{% endblock %}